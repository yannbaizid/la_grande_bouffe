import { IngredientQuantified } from './ingredient-quantified';

describe('IngredientQuantified', () => {
  it('should create an instance', () => {
    expect(new IngredientQuantified()).toBeTruthy();
  });
});
